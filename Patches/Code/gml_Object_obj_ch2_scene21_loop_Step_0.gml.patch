--- a/Code/gml_Object_obj_ch2_scene21_loop_Step_0.gml
+++ b/Code/gml_Object_obj_ch2_scene21_loop_Step_0.gml
@@ -37,17 +37,48 @@
         {
             if (name == "kris")
             {
-                x = obj_queencar.x + 88;
-                y = obj_queencar.y + 20;
-            }
-            if (name == "ralsei")
-            {
-                x = obj_queencar.x + 45;
-                y = obj_queencar.y + 8;
+                if (other.swanboat.swanLong)
+                {
+                    x = obj_mainchara.x;
+                }
+                else
+                {
+                    x = obj_mainchara.x + 8;
+                }
+                y = obj_mainchara.y;
+            }
+            else if (i_ex(global.cinstance[0]) && name == global.cinstance[0].name)
+            {
+                if (other.swanboat.swanLong)
+                {
+                    x = global.cinstance[0].x;
+                }
+                else
+                {
+                    x = global.cinstance[0].x + 15;
+                }
+                y = global.cinstance[0].y;
+            }
+            else if (i_ex(global.cinstance[1]) && name == global.cinstance[1].name)
+            {
+                if (other.swanboat.swanLong)
+                {
+                    x = global.cinstance[1].x;
+                }
+                else
+                {
+                    x = global.cinstance[1].x + 15;
+                }
+                y = global.cinstance[1].y;
             }
         }
     }
-    if (con == 0 && swanboat.x > 240)
+    var longOffset = 0;
+    if (swanboat.swanLong)
+    {
+        longOffset = 40;
+    }
+    if (con == 0 && swanboat.x > (240 - (longOffset / 2)))
     {
         con = 1;
         global.interact = 1;
@@ -306,7 +337,7 @@
         c_sel(kr);
         c_facing("r");
         c_pannable(1);
-        c_panspeed(4, 0, 50);
+        c_pan((camerax() + 203) - longOffset, 0, 50);
         c_msgzurasu(1);
         c_speaker("ralsei");
         c_msgsetloc(0, "\\EJ* W..^1. wow^1, Kris^1! Look at all those houses!/", "obj_ch2_scene21_loop_slash_Step_0_gml_435_0");
@@ -446,6 +477,10 @@
         if (swan_swap == 1)
         {
             swan_cover = scr_dark_marker(swanboat.x, swanboat.y, spr_swanboat_cover);
+            if (swanboat.swanLong)
+            {
+                swan_cover.sprite_index = spr_swanboat_long_cover;
+            }
             swan_cover.image_index = swanboat.image_index;
             swan_cover.image_speed = swanboat.image_speed;
             swan_cover.depth = kr_actor.depth - 10;
@@ -471,13 +506,31 @@
         scr_encountersetup(63);
         battle = instance_create(0, 0, obj_battlecontroller);
         battlehey = instance_create(0, 0, obj_rouxlsbattle_hey);
+        if (swanboat.swanLong)
+        {
+            battle_swan = scr_dark_marker(swanboat.x, swanboat.y, spr_swanboat_long);
+        }
+        else
+        {
-        battle_swan = scr_dark_marker(swanboat.x, swanboat.y, spr_swanboat);
+            battle_swan = scr_dark_marker(swanboat.x, swanboat.y, spr_swanboat);
+        }
         battle_swan.image_index = swanboat.image_index;
         battle_swan.image_speed = swanboat.image_speed;
-        battle_swan.depth = obj_herokris.depth + 1;
+        if (swan_swap == 2)
+        {
+            for (i = 0; i < 3; i++)
+            {
+                if (i_ex(global.charinstance[i]))
+                {
+                    if (global.charinstance[i].depth < swan_cover.depth)
+                    {
+                        swan_cover.depth = global.charinstance[i].depth - 10;
+                    }
+                }
+            }
+        }
+        battle_swan.depth = swan_depth;
         swan_swap = 0;
-        swan_cover.depth = obj_heroralsei.depth - 10;
-        swanboat.depth = swan_depth;
         with (obj_actor)
         {
             visible = 0;
@@ -509,11 +562,14 @@
         con = 43;
         alarm[0] = 30;
         customcon = 0;
+        if (i_ex(obj_herokris))
+        {
-        obj_mainchara.x = obj_herokris.x;
+            obj_mainchara.x = obj_herokris.x;
-        obj_mainchara.y = obj_herokris.y;
+            obj_mainchara.y = obj_herokris.y;
-        kr_actor.x = obj_herokris.x;
+            kr_actor.x = obj_herokris.x;
-        kr_actor.y = obj_herokris.y;
+            kr_actor.y = obj_herokris.y;
-        kr_actor.sprite_index = obj_herokris.sprite_index;
+            kr_actor.sprite_index = obj_herokris.sprite_index;
+        }
         rouxls.sprite_index = spr_rouxls_stone_origin;
         rouxls.image_index = 0;
         rouxls.x += 34;
@@ -939,10 +995,10 @@
         swanboat.pause_y_move = true;
         with (swanboat)
         {
-            scr_move_to_point_over_time(2525, 175, 80);
+            scr_move_to_point_over_time(2525 - longOffset, 175, 80);
         }
     }
-    if (con == 62 && swanboat.x >= 2525 && !d_ex())
+    if (con == 62 && swanboat.x >= (2525 - longOffset) && !d_ex())
     {
         con = 63;
         alarm[0] = 30;
@@ -989,7 +1045,14 @@
             fun = 0;
         }
         var swanboat_npc = instance_create(swanboat.x, swanboat.y, obj_npc_room_animated);
+        if (swanboat.swanLong)
+        {
+            swanboat_npc.sprite_index = spr_swanboat_long;
+        }
+        else
+        {
-        swanboat_npc.sprite_index = spr_swanboat;
+            swanboat_npc.sprite_index = spr_swanboat;
+        }
         swanboat_npc.image_speed = 0.15;
         with (swanboat_npc)
         {
