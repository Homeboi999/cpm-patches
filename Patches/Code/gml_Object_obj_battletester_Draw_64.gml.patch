--- a/Code/gml_Object_obj_battletester_Draw_64.gml
+++ b/Code/gml_Object_obj_battletester_Draw_64.gml
@@ -1,16 +1,16 @@
 setup_encounter = 0;
 if (global.fighting == 0)
 {
-    if (keyboard_check_pressed(ord("1")))
-    {
-        global.encounterno--;
-    }
     if (keyboard_check_pressed(ord("2")))
     {
-        global.encounterno++;
+        global.encounterno--;
     }
     if (keyboard_check_pressed(ord("3")))
     {
+        global.encounterno++;
+    }
+    if (keyboard_check_pressed(ord("1")))
+    {
         global.encounterno -= 5;
     }
     if (keyboard_check_pressed(ord("4")))
@@ -25,31 +25,74 @@
 }
 if (global.fighting == 0)
 {
+    if (global.fighting == 1)
+    {
-    if (keyboard_check_pressed(ord("6")))
+        if (keyboard_check_pressed(ord("6")))
-    {
+        {
-        scr_losechar();
+            scr_losechar();
-        scr_getchar(2);
+            scr_getchar(2);
-        scr_getchar(3);
+            scr_getchar(3);
-    }
+        }
-    if (keyboard_check_pressed(ord("7")))
+        if (keyboard_check_pressed(ord("7")))
-    {
+        {
-        scr_losechar();
+            scr_losechar();
-    }
+        }
-    if (keyboard_check_pressed(ord("8")))
+        if (keyboard_check_pressed(ord("8")))
-    {
+        {
-        scr_losechar();
+            scr_losechar();
-        scr_getchar(3);
+            scr_getchar(3);
-    }
+        }
-    if (keyboard_check_pressed(ord("9")))
+        if (keyboard_check_pressed(ord("9")))
-    {
+        {
-        scr_losechar();
+            scr_losechar();
-        scr_getchar(4);
+            scr_getchar(4);
-        scr_itemget(1);
+            scr_itemget(1);
+        }
+    }
+    else
+    {
+        if (keyboard_check_pressed(ord("6")))
+        {
+            if (global.char[0] >= 4)
+            {
+                global.char[0] = 0;
+            }
+            else
+            {
+                global.char[0]++;
+            }
+        }
+        if (keyboard_check_pressed(ord("7")))
+        {
+            if (global.char[1] >= 4)
+            {
+                global.char[1] = 0;
+            }
+            else
+            {
+                global.char[1]++;
+            }
+        }
+        if (keyboard_check_pressed(ord("8")))
+        {
+            if (global.char[2] >= 4)
+            {
+                global.char[2] = 0;
+            }
+            else
+            {
+                global.char[2]++;
+            }
+        }
     }
     draw_set_color(c_lime);
     scr_84_set_draw_font("main");
-    draw_text(0, 440, string_hash_to_newline("Party:  " + string(global.charname[global.char[0]]) + " " + string(global.charname[global.char[1]]) + " " + string(global.charname[global.char[2]])));
-    draw_text(0, 455, string_hash_to_newline("6: full party. 7:kris only. 8:kris and ralsei 9: kris + noelle"));
+    draw_text(0, 425, string_hash_to_newline("Party:  " + string(global.charname[global.char[0]]) + " / " + string(global.charname[global.char[1]]) + " / " + string(global.charname[global.char[2]])));
+    draw_text(0, 440, string_hash_to_newline("6-8, change party members"));
+}
+else
+{
+    scr_84_set_draw_font("main");
 }
 if (!instance_exists(obj_debug_xy))
 {
@@ -82,7 +125,7 @@
                 }
             }
         }
-        draw_text(300, 0, string_hash_to_newline("Adjust EncounterNo:#1- 2+#3----- 4+++++"));
+        draw_text(300, 0, string_hash_to_newline("Adjust EncounterNo:#2- 1-----#3+ 4+++++"));
         scr_encountersetup(global.encounterno);
     }
     else
