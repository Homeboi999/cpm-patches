--- a/Code/gml_Object_obj_ch2_sceneex2_Create_0.gml
+++ b/Code/gml_Object_obj_ch2_sceneex2_Create_0.gml
@@ -59,18 +59,74 @@
         scr_depth();
     }
     coaster_kris = instance_create(0, cameray() - 500, obj_coaster);
-    coaster_kris.depth = 90000;
-    coaster_kris.visible = 0;
+    with (coaster_kris)
+    {
+        if (scr_modconfig("forceParty") != 1)
+        {
+            HeroType = 2;
+        }
+        else
+        {
+            HeroType = global.char[0];
+        }
+        if (scr_modconfig("coasterType") != 1)
+        {
+            CoasterType = HeroType;
+        }
+        else
+        {
+            CoasterType = 0;
+        }
+        laneID = 0;
+        lane_coaster_y = -2;
+        depth = 90000;
+    }
     coaster_susie = instance_create(0, cameray() - 500, obj_coaster);
-    coaster_susie.type = 1;
-    coaster_susie.depth = 80000;
-    coaster_susie.character_sprite = spr_susie_shock;
-    coaster_susie.visible = 0;
+    with (coaster_susie)
+    {
+        if (scr_modconfig("forceParty") != 1)
+        {
+            HeroType = 2;
+        }
+        else
+        {
+            HeroType = global.char[1];
+        }
+        if (scr_modconfig("coasterType") != 1)
+        {
+            CoasterType = HeroType;
+        }
+        else
+        {
+            CoasterType = 0;
+        }
+        laneID = 1;
+        lane_coaster_y = 13;
+        depth = 80000;
+    }
     coaster_ralsei = instance_create(0, cameray() - 500, obj_coaster);
-    coaster_ralsei.type = 2;
-    coaster_ralsei.depth = 70000;
-    coaster_ralsei.character_sprite = spr_ralsei_hurt_fixed;
-    coaster_ralsei.visible = 0;
+    with (coaster_ralsei)
+    {
+        if (scr_modconfig("forceParty") != 1)
+        {
+            HeroType = 3;
+        }
+        else
+        {
+            HeroType = global.char[2];
+        }
+        if (scr_modconfig("coasterType") != 1)
+        {
+            CoasterType = HeroType;
+        }
+        else
+        {
+            CoasterType = 0;
+        }
+        laneID = 2;
+        lane_coaster_y = 2;
+        depth = 70000;
+    }
     loop_tiles = false;
     bg_loop = layer_get_id("BG_Loop");
     whiteall = scr_dark_marker(-10, -10, spr_pixel_white);
