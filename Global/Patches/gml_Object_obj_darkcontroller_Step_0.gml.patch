--- a/Code/gml_Object_obj_darkcontroller_Step_0.gml
+++ b/Code/gml_Object_obj_darkcontroller_Step_0.gml
@@ -1,3 +1,30 @@
+if (global.interact == 5 && global.menuno == 4)
+{
+    if (global.submenu == 20)
+    {
+        if (button3_p() && threebuffer < 0)
+        {
+            if (scr_modconfig("menuMode") == 0 || scr_modconfig("menuMode") == 2)
+            {
+                selectnoise = 1;
+                threebuffer = 2;
+                partyMenu = instance_create(xx, yy, obj_partymenu);
+                global.submenu = 23;
+            }
+            else
+            {
+                snd_play(snd_cantselect);
+            }
+        }
+    }
+    if (global.submenu == 23)
+    {
+        if (!instance_exists(obj_partymenu))
+        {
+            global.submenu = 20;
+        }
+    }
+}
 xx = __view_get(e__VW.XView, 0);
 yy = __view_get(e__VW.YView, 0);
 if (global.interact == 5)
@@ -5,6 +32,19 @@
     charcon = 1;
     if (global.submenu == 5 || global.submenu == 22)
     {
+        if (global.char[global.submenucoord[global.submenu]] == 0)
+        {
+            var firstslot = 0;
+            for (var pos = 0; pos < 3; pos++)
+            {
+                if (global.char[firstslot] != 0)
+                {
+                    break;
+                }
+                firstslot = (pos + 1) % 3;
+            }
+            global.submenucoord[global.submenu] = firstslot;
+        }
         global.charselect = global.submenucoord[global.submenu];
         global.faceaction[0] = 0;
         global.faceaction[1] = 0;
@@ -13,26 +53,12 @@
         if (left_p())
         {
             movenoise = 1;
-            if (global.submenucoord[global.submenu] > 0)
-            {
-                global.submenucoord[global.submenu] -= 1;
-            }
-            else
-            {
-                global.submenucoord[global.submenu] = chartotal - 1;
-            }
+            global.submenucoord[global.submenu] = scr_menucharselect(-1);
         }
         if (right_p())
         {
             movenoise = 1;
-            if (global.submenucoord[global.submenu] < (chartotal - 1))
-            {
-                global.submenucoord[global.submenu] += 1;
-            }
-            else
-            {
-                global.submenucoord[global.submenu] = 0;
-            }
+            global.submenucoord[global.submenu] = scr_menucharselect(1);
         }
         if (button1_p() && onebuffer < 0 && twobuffer < 0)
         {
@@ -851,9 +877,10 @@
             }
             if (button1_p() && onebuffer < 0)
             {
+                var emptyparty = global.char[0] == 0 && global.char[1] == 0 && global.char[2] == 0;
                 if (global.spellusable[charcoord][global.submenucoord[21]] == 1 && global.tension >= global.spellcost[charcoord][global.submenucoord[21]])
                 {
-                    if (global.spelltarget[charcoord][global.submenucoord[21]] == 1)
+                    if (global.spelltarget[charcoord][global.submenucoord[21]] == 1 && !emptyparty)
                     {
                         global.submenu = 22;
                         onebuffer = 2;
@@ -879,28 +906,22 @@
             if (left_p())
             {
                 movenoise = 1;
-                global.submenucoord[20] -= 1;
-                if (global.submenucoord[20] < 0)
-                {
-                    global.submenucoord[20] = chartotal - 1;
-                }
-                if (chartotal >= 2)
+                var newcoord = scr_menucharselect(-1);
+                if (newcoord != global.submenucoord[global.submenu])
                 {
                     dograndom = ceil(random(100));
                 }
+                global.submenucoord[global.submenu] = newcoord;
             }
             if (right_p())
             {
                 movenoise = 1;
-                global.submenucoord[20] += 1;
-                if (global.submenucoord[20] > (chartotal - 1))
-                {
-                    global.submenucoord[20] = 0;
-                }
-                if (chartotal >= 2)
+                var newcoord = scr_menucharselect(1);
+                if (newcoord != global.submenucoord[global.submenu])
                 {
                     dograndom = ceil(random(100));
                 }
+                global.submenucoord[global.submenu] = newcoord;
             }
             global.charselect = global.submenucoord[20];
             if (button1_p() && onebuffer < 0)
@@ -1101,10 +1122,16 @@
         }
         if (global.submenu == 2)
         {
+            var emptyparty = global.char[0] == 0 && global.char[1] == 0 && global.char[2] == 0;
             if (button1_p() && onebuffer < 0 && twobuffer < 0)
             {
                 onebuffer = 3;
                 scr_iteminfo(global.item[global.submenucoord[2]]);
+                if (emptyparty)
+                {
+                    itemtarget = 0;
+                    snd_play(snd_cantselect);
+                }
                 if (itemtarget == 1)
                 {
                     selectnoise = 1;
@@ -1486,20 +1513,12 @@
             if (left_p())
             {
                 movenoise = 1;
-                global.submenucoord[10] -= 1;
-                if (global.submenucoord[10] < 0)
-                {
-                    global.submenucoord[10] = chartotal - 1;
-                }
+                global.submenucoord[global.submenu] = scr_menucharselect(-1);
             }
             if (right_p())
             {
                 movenoise = 1;
-                global.submenucoord[10] += 1;
-                if (global.submenucoord[10] > (chartotal - 1))
-                {
-                    global.submenucoord[10] = 0;
-                }
+                global.submenucoord[global.submenu] = scr_menucharselect(1);
             }
             global.charselect = global.submenucoord[10];
             if (button1_p() && onebuffer < 0)
@@ -1562,6 +1581,15 @@
             selectnoise = 1;
             onebuffer = 2;
             global.menuno = global.menucoord[0] + 1;
+            var firstslot = 0;
+            for (var pos = 0; pos < 3; pos++)
+            {
+                if (global.char[firstslot] != 0)
+                {
+                    break;
+                }
+                firstslot = (pos + 1) % 3;
+            }
             if (global.menuno == 1)
             {
                 global.submenu = 1;
@@ -1576,7 +1604,7 @@
                 scr_armorinfo_all();
                 scr_weaponinfo_mine();
                 scr_armorinfo_mine();
-                global.submenucoord[10] = 0;
+                global.submenucoord[10] = firstslot;
                 global.submenucoord[11] = 0;
                 global.submenucoord[12] = 0;
                 global.submenucoord[13] = 0;
@@ -1593,6 +1621,10 @@
             if (global.menuno == 4)
             {
                 global.submenu = 20;
+                if (global.char[global.submenucoord[20]] == 0)
+                {
+                    global.submenucoord[20] = firstslot;
+                }
                 scr_spellinfo_all();
             }
             if (global.menuno == 5)
