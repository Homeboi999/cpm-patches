--- a/Code/gml_GlobalScript_scr_maincharacters_actors.gml
+++ b/Code/gml_GlobalScript_scr_maincharacters_actors.gml
@@ -10,11 +10,16 @@
     {
         visible = 0;
     }
+    var actorX = 0;
+    var actorY = 0;
+    var su_exists = false;
+    var ra_exists = false;
+    var no_exists = false;
     for (__jj = 0; __jj < array_length_1d(global.cinstance); __jj++)
     {
         if (i_ex(global.cinstance[__jj]))
         {
-            if (global.cinstance[__jj].name == "susie")
+            if (global.cinstance[__jj].name == "susie" && !su_exists)
             {
                 actor_count++;
                 su = __jj + 1;
@@ -22,12 +27,13 @@
                 scr_actor_setup(su, su_actor, "susie");
                 su_actor.sprite_index = global.cinstance[__jj].sprite_index;
                 su_actor.depth = global.cinstance[__jj].depth;
+                su_exists = true;
                 with (global.cinstance[__jj])
                 {
                     visible = 0;
                 }
             }
-            if (global.cinstance[__jj].name == "ralsei")
+            if (global.cinstance[__jj].name == "ralsei" && !ra_exists)
             {
                 actor_count++;
                 ra = __jj + 1;
@@ -35,12 +41,13 @@
                 scr_actor_setup(ra, ra_actor, "ralsei");
                 ra_actor.sprite_index = global.cinstance[__jj].sprite_index;
                 ra_actor.depth = global.cinstance[__jj].depth;
+                ra_exists = true;
                 with (global.cinstance[__jj])
                 {
                     visible = 0;
                 }
             }
-            if (global.cinstance[__jj].name == "noelle")
+            if (global.cinstance[__jj].name == "noelle" && !no_exists)
             {
                 actor_count++;
                 no = __jj + 1;
@@ -48,12 +55,43 @@
                 scr_actor_setup(no, no_actor, "noelle");
                 no_actor.sprite_index = global.cinstance[__jj].sprite_index;
                 no_actor.depth = global.cinstance[__jj].depth;
+                no_exists = true;
                 with (global.cinstance[__jj])
                 {
                     visible = 0;
                 }
             }
         }
+    }
+    if (!su_exists)
+    {
+        actor_count++;
+        su = actor_count;
+        su_actor = instance_create(obj_mainchara.x, obj_mainchara.y, obj_actor);
+        scr_actor_setup(su, su_actor, "susie");
+        su_actor.sprite_index = spr_susied_dark;
+        su_actor.depth = obj_mainchara.depth + 5;
+        su_actor.visible = 0;
+    }
+    if (!ra_exists)
+    {
+        actor_count++;
+        ra = actor_count;
+        ra_actor = instance_create(obj_mainchara.x, obj_mainchara.y, obj_actor);
+        scr_actor_setup(ra, ra_actor, "ralsei");
+        ra_actor.sprite_index = spr_ralseid;
+        ra_actor.depth = obj_mainchara.depth + 10;
+        ra_actor.visible = 0;
+    }
+    if (!no_exists)
+    {
+        actor_count++;
+        no = actor_count;
+        no_actor = instance_create(obj_mainchara.x, obj_mainchara.y, obj_actor);
+        scr_actor_setup(no, no_actor, "noelle");
+        no_actor.sprite_index = spr_noelle_walk_down_dw;
+        no_actor.depth = obj_mainchara.depth + 15;
+        no_actor.visible = 0;
     }
 }
 
