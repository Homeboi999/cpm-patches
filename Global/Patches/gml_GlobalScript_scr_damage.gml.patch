--- a/Code/gml_GlobalScript_scr_damage.gml
+++ b/Code/gml_GlobalScript_scr_damage.gml
@@ -11,9 +11,14 @@
                 __element = element;
             }
         }
+        if (global.char[0] == 0 && global.char[1] == 0 && global.char[2] == 0)
+        {
+            scr_gameover();
+            exit;
+        }
         if (target < 3)
         {
-            if (global.hp[global.char[target]] <= 0)
+            if (global.char[target] == 0 || global.hp[global.char[target]] <= 0)
             {
                 scr_randomtarget_old();
                 target = mytarget;
