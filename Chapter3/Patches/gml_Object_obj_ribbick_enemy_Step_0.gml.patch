--- a/Code/gml_Object_obj_ribbick_enemy_Step_0.gml
+++ b/Code/gml_Object_obj_ribbick_enemy_Step_0.gml
@@ -607,12 +607,56 @@
             extimer = 0;
         }
     }
+    if (actingnoe == 1 && actconnoe == 1)
+    {
+        if (simultotal == 1)
+        {
+            msgset(0, "* Noelle kept herself clean! The enemies became TIRED.../%");
+            scr_battletext_default();
+            actconnoe = 20;
+        }
+        else
+        {
+            var rand = choose(0, 1, 2);
+            if (rand == 0)
+            {
+                msgset(0, "* Noelle dusts herself off!");
+            }
+            if (rand == 1)
+            {
+                msgset(0, "* Noelle wears spare clothes!");
+            }
+            if (rand == 2)
+            {
+                msgset(0, "* Noelle puts gloves on!");
+            }
+            scr_simultext("noelle");
+            if (simulordernoe == 0)
+            {
+                actconnoe = 20;
+            }
+            else
+            {
+                actconnoe = 0;
+            }
+        }
+        with (obj_ribbick_enemy)
+        {
+            if (global.monsterstatus[myself] != 1)
+            {
+                scr_monster_make_tired(myself);
+            }
+        }
+        actingnoe = 0;
+    }
-    if (actcon == 20 || actconsus == 20 || actconral == 20)
+    if (actcon == 20 || actconsus == 20 || actconral == 20 || actconnoe == 20)
     {
+        scr_nextact_simul();
         if (scr_terminate_writer())
         {
             actconsus = -1;
             actconral = -1;
+            actconnoe = -1;
             actcon = 1;
         }
     }
