--- a/Code/gml_Object_obj_shutta_photo_controller_Other_17.gml
+++ b/Code/gml_Object_obj_shutta_photo_controller_Other_17.gml
@@ -128,6 +128,19 @@
             }
             global.flag[515] = 1;
         }
+        else if (got_noelle && closest_hero == "noelle")
+        {
+            endmessage = message_nicephoto;
+            if (enemy.amiabossfight == true)
+            {
+                obj_shutta_photo_controller.endmessage = message_noelle;
+                if (global.flag[516] > 0)
+                {
+                    obj_shutta_photo_controller.endmessage = message_noelle2;
+                }
+            }
+            global.flag[516] = 1;
+        }
         else if (place_meeting(x, y, _lancer) && collision_circle(x, y, reticle_size + 33, _lancer, false, true) && _lancer.sprite_index == spr_ch3_dw_shutta_lancer_tongue)
         {
             enemy.photosuccessballoon = 1;
