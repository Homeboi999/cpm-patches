--- a/Code/gml_Object_obj_shutta_photo_controller_Other_12.gml
+++ b/Code/gml_Object_obj_shutta_photo_controller_Other_12.gml
@@ -123,6 +123,20 @@
             }
             global.flag[515] = 1;
         }
+        else if (got_noelle && closest_hero == "noelle")
+        {
+            enemy.photosuccesscount++;
+            endmessage = message_nicephoto;
+            if (enemy.amiabossfight == true)
+            {
+                obj_shutta_photo_controller.endmessage = message_noelle;
+                if (global.flag[516] > 0)
+                {
+                    obj_shutta_photo_controller.endmessage = message_noelle2;
+                }
+            }
+            global.flag[516] = 1;
+        }
         else if (enemy.sprite_index == spr_shutta_pose && enemy.image_index >= 7 && place_meeting(x, y, enemy) && collision_circle(x, y, reticle_size, obj_shutta_shine, false, true))
         {
             if (enemy.amiabossfight == true)
