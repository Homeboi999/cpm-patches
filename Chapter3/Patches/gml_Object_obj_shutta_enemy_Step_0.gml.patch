--- a/Code/gml_Object_obj_shutta_enemy_Step_0.gml
+++ b/Code/gml_Object_obj_shutta_enemy_Step_0.gml
@@ -1,27 +1,24 @@
-with (obj_heroparent)
+if (instance_exists(global.charinstance[0]))
 {
-    if (object_index == obj_herokris)
+    with (global.charinstance[0])
     {
         depth = 160;
     }
-    if (object_index == obj_herosusie)
+}
+if (instance_exists(global.charinstance[1]))
+{
+    with (global.charinstance[1])
     {
         depth = 157;
     }
-    if (object_index == obj_heroralsei)
+}
+if (instance_exists(global.charinstance[2]))
+{
+    with (global.charinstance[2])
     {
         depth = 154;
     }
 }
-if (!i_ex(obj_herosusie) || !i_ex(obj_heroralsei))
-{
-    if (debugvar == false && scr_debug())
-    {
-        scr_debug_print("You need Susie and Ralsei for this battle");
-        debugvar = true;
-    }
-    exit;
-}
 if (endbattle == true && !i_ex(obj_writer))
 {
     scr_spare(myself);
@@ -154,36 +151,63 @@
                     lancerfailcount++;
                 }
             }
-            else if (photoactcount == 4)
-            {
-                if (photosuccessballoon == 1)
-                {
-                    msgsetloc(0, "Ah, the beautiful&wildlife photo of&the Susie in her&natural environment.../%", "obj_shutta_enemy_slash_Step_0_gml_84_0");
-                    ballooncon = 7;
-                    balloonend = 0;
-                }
-                else
-                {
-                    msgsetloc(0, "Ah, the Kris has&failed the Toilet&Challenge./%", "obj_shutta_enemy_slash_Step_0_gml_85_0");
-                    ballooncon = 8;
-                    balloonend = 0;
-                    toiletfailcount++;
-                }
-            }
-            else if (photoactcount == 5)
-            {
-                if (photosuccessballoon == 1)
-                {
-                    msgsetloc(0, "Ooo la la. The&Ralsei!&I would recognize&that silhouette.../%", "obj_shutta_enemy_slash_Step_0_gml_89_0");
-                    ballooncon = 9;
-                    balloonend = 0;
-                }
-                else
-                {
-                    msgsetloc(0, "Ooo la la. Kris,&don't you recognize&The Ralsei, your&dear companion,&your friendship,&little flossy boy,&the bimbito./%", "obj_shutta_enemy_slash_Step_0_gml_90_0");
-                    ballooncon = 10;
-                    balloonend = 0;
-                    ralseifailcount++;
+            else if (photoactcount == 4 || photoactcount == 5)
+            {
+                if (photosuccessballoon == 1)
+                {
+                    switch (curpartyphoto)
+                    {
+                        case 2:
+                            msgsetloc(0, "Ah, the beautiful&wildlife photo of&the Susie in her&natural environment.../%", "obj_shutta_enemy_slash_Step_0_gml_84_0");
+                            ballooncon = 7;
+                            balloonend = 0;
+                            break;
+                        case 3:
+                            msgsetloc(0, "Ooo la la. The&Ralsei!&I would recognize&that silhouette.../%", "obj_shutta_enemy_slash_Step_0_gml_89_0");
+                            ballooncon = 9;
+                            balloonend = 0;
+                            break;
+                        case 4:
+                            msgset(0, "[NOELLE SUCCESS]/%");
+                            ballooncon = 14;
+                            balloonend = 0;
+                            break;
+                        default:
+                            msgset(0, "Ooh la la. A&candid photo of&your wonderful&companion.../%");
+                            ballooncon = 16;
+                            balloonend = 0;
+                            break;
+                    }
+                }
+                else
+                {
+                    switch (curpartyphoto)
+                    {
+                        case 2:
+                            msgsetloc(0, "Ah, the Kris has&failed the Toilet&Challenge./%", "obj_shutta_enemy_slash_Step_0_gml_85_0");
+                            ballooncon = 8;
+                            balloonend = 0;
+                            toiletfailcount++;
+                            break;
+                        case 3:
+                            msgsetloc(0, "Ooo la la. Kris,&don't you recognize&The Ralsei, your&dear companion,&your friendship,&little flossy boy,&the bimbito./%", "obj_shutta_enemy_slash_Step_0_gml_90_0");
+                            ballooncon = 10;
+                            balloonend = 0;
+                            ralseifailcount++;
+                            break;
+                        case 4:
+                            msgset(0, "[NOELLE FAIL]/%");
+                            ballooncon = 15;
+                            balloonend = 0;
+                            noellefailcount++;
+                            break;
+                        default:
+                            msgset(0, "Ooh la la. I&don't recognize&the subject of&this photo.../%");
+                            ballooncon = 17;
+                            balloonend = 0;
+                            luigifailcount++;
+                            break;
+                    }
                 }
             }
             else if (photoactcount == 6)
@@ -410,6 +434,30 @@
                 ballooncon = 0;
                 balloonend = 1;
             }
+            else if (ballooncon == 14)
+            {
+                msgset(0, "[NOELLE SUCCESS 2]/%");
+                ballooncon = 0;
+                balloonend = 1;
+            }
+            else if (ballooncon == 15)
+            {
+                msgset(0, "[NOELLE FAIL 2]/%");
+                ballooncon = 0;
+                balloonend = 1;
+            }
+            else if (ballooncon == 16)
+            {
+                msgset(0, "Incredible&how they stand&out in the&crowd./%");
+                ballooncon = 0;
+                balloonend = 1;
+            }
+            else if (ballooncon == 17)
+            {
+                msgset(0, "Couldn't you&find your&companions&among the crowd?/%");
+                ballooncon = 0;
+                balloonend = 1;
+            }
             talked = 0.7;
             global.typer = 50;
             scr_enemyblcon(x - 5, global.monstery[myself], 10);
@@ -590,38 +638,39 @@
     {
         photoacttext = stringsetloc("* Photo the most bullets!", "obj_shutta_enemy_slash_Step_0_gml_314_0");
     }
-    if (actingral == 2 && filter == false)
-    {
-        with (obj_shutta_enemy)
-        {
-            filter = true;
-        }
-        if (global.flag[512] == 0 && simultotal == 1)
-        {
-            msgsetloc(0, "* Ralsei adds a filter to the photo! It's easier to center photos!!/%", "obj_shutta_enemy_slash_Step_0_gml_181_0");
-            global.flag[512] = 1;
-        }
-        else
-        {
-            msgsetloc(0, "* Ralsei adds a filter!/%", "obj_shutta_enemy_slash_Step_0_gml_184_0");
-        }
-        scr_battletext_default();
-        exit;
-    }
-    if (acting == 2 && actcon == 0 && !i_ex(obj_writer))
-    {
-        fastphoto = false;
-        acting = 3.1;
-    }
-    if (acting == 3 && actcon == 0 && !i_ex(obj_writer))
+    var actnum = 2;
+    if (scr_havechar(3))
+    {
+        if (acting == actnum && actcon == 0 && !i_ex(obj_writer))
+        {
+            fastphoto = false;
+            acting = 3.1;
+        }
+        actnum++;
+    }
+    if (acting == actnum && actcon == 0 && !i_ex(obj_writer))
     {
         fastphoto = true;
         acting = 3.1;
     }
+    actnum++;
+    if (scr_havechar(2))
+    {
-    if (acting == 4 && actcon == 0 && !i_ex(obj_writer))
+        if (acting == actnum && actcon == 0 && !i_ex(obj_writer))
-    {
+        {
-        powerphoto = true;
+            powerphoto = true;
-        acting = 3.1;
+            acting = 3.1;
+        }
+        actnum++;
+    }
+    if (scr_havechar(4))
+    {
+        if (acting == actnum && actcon == 0 && !i_ex(obj_writer))
+        {
+            fancyphoto = true;
+            acting = 3.1;
+        }
+        actnum++;
     }
     if (acting == 3.1 && actcon == 0 && !i_ex(obj_writer))
     {
@@ -633,6 +682,8 @@
         var _rotationtring = stringsetloc("* Take a photo of Shuttah's Smile!/%", "obj_shutta_enemy_slash_Step_0_gml_353_0");
         var _ralseitring = stringsetloc("* Take a photo of Ralsei!/%", "obj_shutta_enemy_slash_Step_0_gml_354_0");
         var _bullettring = stringsetloc("* Photo as many bullets as you can!", "obj_shutta_enemy_slash_Step_0_gml_355_0");
+        var _xmasstring = stringset("* Take a photo of Noelle!&* TODO: make a more unique thing./%");
+        var _findluigistring = stringset("* Take a photo of a party member!/%");
         if (amiabossfight == true)
         {
             if (photoactcount == 1)
@@ -647,13 +698,40 @@
             {
                 msgset(0, _lancerstring);
             }
-            if (photoactcount == 4)
-            {
-                msgset(0, _toiletstring);
-            }
-            if (photoactcount == 5)
-            {
-                msgset(0, _ralseitring);
+            if (photoactcount == 4 || photoactcount == 5)
+            {
+                var index = 0;
+                if (photoactcount == 5)
+                {
+                    index = 1;
+                }
+                if (photoactcount == 5 && partyphotos[0] == partyphotos[1])
+                {
+                    msgset(0, _findluigistring);
+                    curpartyphoto = 0;
+                }
+                else
+                {
+                    switch (partyphotos[index])
+                    {
+                        case 2:
+                            msgset(0, _toiletstring);
+                            curpartyphoto = 2;
+                            break;
+                        case 3:
+                            msgset(0, _ralseitring);
+                            curpartyphoto = 3;
+                            break;
+                        case 4:
+                            msgset(0, _xmasstring);
+                            curpartyphoto = 4;
+                            break;
+                        default:
+                            msgset(0, _findluigistring);
+                            curpartyphoto = 0;
+                            break;
+                    }
+                }
             }
             if (photoactcount == 6)
             {
@@ -674,7 +752,34 @@
             if (_rand == 0)
             {
                 photoactcount = 5;
+                if (!scr_havechar(3) && !scr_havechar(4))
+                {
+                    curpartyphoto = 0;
+                }
+                else if (!scr_havechar(4))
+                {
+                    curpartyphoto = 3;
+                }
+                else if (!scr_havechar(3))
+                {
+                    curpartyphoto = 4;
+                }
+                else
+                {
+                    curpartyphoto = choose(3, 4);
+                }
+                if (curpartyphoto == 3)
+                {
-                msgset(0, _ralseitring);
+                    msgset(0, _ralseitring);
+                }
+                else if (curpartyphoto == 4)
+                {
+                    msgset(0, _xmasstring);
+                }
+                else
+                {
+                    msgset(0, _findluigistring);
+                }
             }
             if (_rand == 1)
             {
@@ -697,6 +802,7 @@
         _controller.fastphoto = true;
         _controller.posemax = 50 + irandom(20);
         _controller.powerphoto = powerphoto;
+        _controller.fancyphoto = fancyphoto;
         if (filter)
         {
             _controller.centered_distance = 47;
@@ -710,6 +816,10 @@
         {
             _controller.timer_duration = 150;
         }
+        if (fancyphoto == true)
+        {
+            _controller.timer_duration = 225;
+        }
         if (amiabossfight == true && photoactcount == 0)
         {
             _controller.timer_duration = 540;
@@ -886,16 +996,66 @@
             scr_mercyadd(myself, 35);
         }
     }
+    if (actingnoe == 1 && actconnoe == 1)
+    {
+        var _text = "_";
+        var rand = choose(0, 1, 2);
+        if (rand == 0)
+        {
+            _text = stringset("* Noelle adjusts the focus!");
+        }
+        if (rand == 1)
+        {
+            _text = stringset("* Noelle cleans the camera lens!");
+        }
+        if (rand == 2)
+        {
+            _text = stringset("* Noelle poses with a festive prop!");
+        }
+        if (simultotal == 1)
+        {
+            msgset(0, _text + "/%");
+            scr_battletext_default();
+            with (obj_face)
+            {
+                instance_destroy();
+            }
+            actconnoe = 20;
+        }
+        else
+        {
+            msgset(0, _text);
+            scr_simultext("noelle");
+            if (simulordernoe == 0)
+            {
+                actconnoe = 20;
+            }
+            else
+            {
+                actconnoe = 0;
+            }
+        }
+        actingnoe = 0;
+        if (amiabossfight == true)
+        {
+            scr_mercyadd(myself, 5);
+        }
+        else
+        {
+            scr_mercyadd(myself, 30);
+        }
+    }
     if (actingral == 2 && actconral == 1)
     {
         actcon = 1;
         actingral = 0;
         actconral = 0;
     }
-    if (actcon == 20 || actconsus == 20 || actconral == 20)
+    if (actcon == 20 || actconsus == 20 || actconral == 20 || actconnoe == 20)
     {
         if (scr_terminate_writer())
         {
+            actconnoe = -1;
             actconsus = -1;
             actconral = -1;
             actcon = 1;
