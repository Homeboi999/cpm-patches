--- a/Code/gml_Object_obj_heroparent_Other_10.gml
+++ b/Code/gml_Object_obj_heroparent_Other_10.gml
@@ -25,15 +25,15 @@
                 damage = ceil(damage * obj_knight_enemy.damagereduction);
                 if (object_index == obj_herokris)
                 {
-                    if (global.hp[2] < 0 && global.hp[3] < 0)
+                    if (scr_heroupcount() == 3)
                     {
                         damage *= 2;
                     }
-                    else if (global.hp[2] < 0 || global.hp[3] < 0)
+                    if (scr_heroupcount() == 2)
                     {
                         damage *= 1;
                     }
-                    else
+                    if (scr_heroupcount() <= 1)
                     {
                         damage = round(damage * 0.5);
                     }
