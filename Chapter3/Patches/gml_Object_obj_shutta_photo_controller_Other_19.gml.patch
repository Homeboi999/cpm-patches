--- a/Code/gml_Object_obj_shutta_photo_controller_Other_19.gml
+++ b/Code/gml_Object_obj_shutta_photo_controller_Other_19.gml
@@ -90,6 +90,19 @@
             }
             global.flag[515] = 1;
         }
+        else if (got_noelle && closest_hero == "noelle")
+        {
+            endmessage = message_nicephoto;
+            if (enemy.amiabossfight == true)
+            {
+                obj_shutta_photo_controller.endmessage = message_noelle;
+                if (global.flag[516] > 0)
+                {
+                    obj_shutta_photo_controller.endmessage = message_noelle2;
+                }
+            }
+            global.flag[516] = 1;
+        }
         else if (_trueralsei != -1 && place_meeting(x, y, _trueralsei) && collision_circle(x, y, reticle_size + 35, _trueralsei, false, true))
         {
             enemy.photosuccessballoon = 1;
