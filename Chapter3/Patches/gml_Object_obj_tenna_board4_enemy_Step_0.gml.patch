--- a/Code/gml_Object_obj_tenna_board4_enemy_Step_0.gml
+++ b/Code/gml_Object_obj_tenna_board4_enemy_Step_0.gml
@@ -1,30 +1,19 @@
-if (!i_ex(obj_herosusie) || !i_ex(obj_heroralsei))
-{
-    if (debugvar == false && scr_debug())
-    {
-        scr_debug_print("You need Susie and Ralsei for this battle");
-        debugvar = true;
-    }
-    exit;
-}
 if (intro >= 1)
 {
     obj_battlecontroller.oopsallacts = 1;
 }
 if (gachainit == 0)
 {
-    kris_gacha = instance_create(0, 0, obj_tenna_board4_gacha);
-    kris_gacha.actor_target = 1410;
-    kris_gacha.mode = 3;
-    kris_gacha.depth = obj_herokris.depth - 1;
-    susie_gacha = instance_create(0, 0, obj_tenna_board4_gacha);
-    susie_gacha.actor_target = 1412;
-    susie_gacha.mode = 3;
-    susie_gacha.depth = obj_herosusie.depth - 1;
-    ralsei_gacha = instance_create(0, 0, obj_tenna_board4_gacha);
-    ralsei_gacha.actor_target = 1413;
-    ralsei_gacha.mode = 3;
-    ralsei_gacha.depth = obj_heroralsei.depth - 1;
+    for (var i = 0; i < 3; i++)
+    {
+        if (i_ex(global.charinstance[i]))
+        {
+            hero_gacha[i] = instance_create(0, 0, obj_tenna_board4_gacha);
+            hero_gacha[i].actor_target = global.charinstance[i];
+            hero_gacha[i].mode = 3;
+            hero_gacha[i].depth = global.charinstance[i].depth - 1;
+        }
+    }
     gachainit = 1;
 }
 if (ilovetvcount > 3)
@@ -476,7 +465,11 @@
     }
     if (endcon == 0)
     {
-        if (((global.hp[1] < 1 && global.hp[2] < 1 && global.hp[3] < 1) && global.turntimer > 0) || ((global.hp[1] < 1 || global.hp[2] < 1 || global.hp[3] < 1) && global.turntimer < 2))
+        var hero1down = global.char[0] != 0 && global.hp[global.char[0]] < 1;
+        var hero2down = global.char[1] != 0 && global.hp[global.char[1]] < 1;
+        var hero3down = global.char[2] != 0 && global.hp[global.char[2]] < 1;
+        var emptyhit = global.hp[0] < 0;
+        if ((((hero1down && hero2down && hero3down) || emptyhit) && global.turntimer > 0) || ((hero1down || hero2down || hero3down) && global.turntimer < 2))
         {
             with (obj_battlecontroller)
             {
@@ -522,11 +515,15 @@
                 killactive = 1;
             }
         }
-        for (i = 0; i < 3; i++)
+        for (var i = 0; i < 3; i++)
         {
-            if (global.hp[i + 1] <= 0)
-            {
-                var _hltarget = global.char[i];
+            var _hltarget = global.char[i];
+            if (_hltarget == 0)
+            {
+                continue;
+            }
+            if (global.hp[_hltarget] <= 0)
+            {
                 var _curhp = global.hp[_hltarget];
                 if (_curhp <= 0)
                 {
@@ -559,7 +556,8 @@
                 }
             }
         }
+        global.hp[0] = 0;
-        for (i = 0; i < 3; i++)
+        for (var i = 0; i < 3; i++)
         {
             with (global.charinstance[i])
             {
@@ -640,26 +638,37 @@
     yy = __view_get(e__VW.YView, 0);
     if (acting == 1 && actcon == 0)
     {
+        if (obj_battlecontroller.chartotal <= 1)
+        {
+            switch (global.char[global.currentactingchar])
+            {
+                case 1:
+                default:
+                    msgset(0, "* You said I Love TV to Tenna!/%");
+                    break;
+                case 2:
+                    msgset(0, "* Susie said I Love TV to Tenna!/%");
+                    break;
+                case 3:
+                    msgset(0, "* Ralsei said I Love TV to Tenna!/%");
+                    break;
+                case 4:
+                    msgset(0, "* Noelle said I Love TV to Tenna!/%");
+                    break;
+            }
+        }
+        else
+        {
-        msgsetloc(0, "* Everyone said I Love TV to Tenna!/%", "obj_tenna_board4_enemy_slash_Step_0_gml_410_0");
+            msgsetloc(0, "* Everyone said I Love TV to Tenna!/%", "obj_tenna_board4_enemy_slash_Step_0_gml_410_0");
+        }
         scr_battletext_default();
         acting = 1.1;
     }
     if (acting == 1.1 && actcon == 0 && !i_ex(obj_writer))
     {
-        ilovetv_increase = 3;
-        ilovetvcount += 3;
-        if (ilovetvcount == 2)
-        {
-            scr_wincombat();
-            with (obj_battlecontroller)
-            {
-                skipvictory = 1;
-            }
-        }
-        else
-        {
-            acting = 1.2;
-        }
+        ilovetv_increase = obj_battlecontroller.chartotal;
+        ilovetvcount += obj_battlecontroller.chartotal;
+        acting = 1.2;
     }
     if (acting == 1.2 && actcon == 0)
     {
@@ -823,7 +832,29 @@
     }
     if (acting == 2 && actcon == 0)
     {
+        if (obj_battlecontroller.chartotal <= 1)
+        {
+            switch (global.char[global.currentactingchar])
+            {
+                case 1:
+                default:
+                    msgset(0, "* You resisted!/%");
+                    break;
+                case 2:
+                    msgset(0, "* Susie resisted!/%");
+                    break;
+                case 3:
+                    msgset(0, "* Ralsei resisted!/%");
+                    break;
+                case 4:
+                    msgset(0, "* Noelle resisted!/%");
+                    break;
+            }
+        }
+        else
+        {
-        msgsetloc(0, "* Everyone resisted!", "obj_tenna_board4_enemy_slash_Step_0_gml_522_0");
+            msgsetloc(0, "* Everyone resisted!", "obj_tenna_board4_enemy_slash_Step_0_gml_522_0");
+        }
         scr_battletext_default();
         resistcount++;
         if (resistcount == 1)
@@ -1008,6 +1039,7 @@
     }
     if (actcon == 20 || actconsus == 20 || actconral == 20)
     {
+        scr_nextact_simul();
         if (scr_terminate_writer())
         {
             actconsus = -1;
