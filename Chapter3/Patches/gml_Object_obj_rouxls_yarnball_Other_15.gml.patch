--- a/Code/gml_Object_obj_rouxls_yarnball_Other_15.gml
+++ b/Code/gml_Object_obj_rouxls_yarnball_Other_15.gml
@@ -2,14 +2,14 @@
 {
     damage = 50;
     element = 6;
-    if (obj_rouxls_ch3_enemy.ralseiyarnhp > 0 && global.hp[3] > 0)
+    if (obj_rouxls_ch3_enemy.ralseiyarnhp > 0 && scr_havechar(3) && global.hp[3] > 0)
     {
         damage = floor(global.hp[3] / 2);
         if (damage > 40)
         {
             damage = 40;
         }
-        target = 2;
+        target = scr_findchar(3);
     }
     else
     {
@@ -25,7 +25,7 @@
     }
     if (global.hp[3] > 0)
     {
-        target = 2;
+        target = scr_findchar(3);
         instance_destroy();
         snd_play(snd_bump);
         obj_rouxls_ch3_enemy.ralseiyarnhp--;
@@ -34,14 +34,17 @@
         }
         if (obj_rouxls_ch3_enemy.ralseiyarnhp == 0)
         {
-            scr_battle_sprite_set("ralsei", spr_ralseib_yarn_2, 0, 0);
             obj_rouxls_ch3_enemy.shadowmengetfiredcon = 1;
             obj_rouxls_ch3_enemy.destroywriter = false;
             with (obj_battlecontroller)
             {
                 noreturn = 1;
             }
+            if (scr_havechar(3))
+            {
+                scr_battle_sprite_set("ralsei", spr_ralseib_yarn_2, 0, 0);
-            snd_play_x(snd_awkward, 0.67, 1);
+                snd_play_x(snd_awkward, 0.67, 1);
+            }
             scr_shakescreen();
         }
     }
