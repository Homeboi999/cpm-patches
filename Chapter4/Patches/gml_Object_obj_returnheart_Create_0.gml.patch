--- a/Code/gml_Object_obj_returnheart_Create_0.gml
+++ b/Code/gml_Object_obj_returnheart_Create_0.gml
@@ -2,15 +2,20 @@
 shift = 1;
 image_alpha = 1;
 flytime = 8;
-if ((global.chapter == 4 && i_ex(obj_hammer_of_justice_enemy)) || (global.chapter == 4 && i_ex(obj_sound_of_justice_enemy) && !i_ex(obj_herokris)))
+if ((global.chapter == 4 && i_ex(obj_hammer_of_justice_enemy)) || (global.chapter == 4 && i_ex(obj_sound_of_justice_enemy) && !i_ex(obj_herokris) && i_ex(obj_herosusie)))
 {
     distx = obj_herosusie.x - 150;
     disty = obj_herosusie.y + 40;
 }
-else
+else if (i_ex(obj_herokris))
 {
     distx = obj_herokris.x + 10;
     disty = obj_herokris.y + 40;
+}
+else
+{
+    distx = x;
+    disty = y;
 }
 dist = point_distance(x, y, distx, disty);
 move_towards_point(distx, disty, dist / flytime);
