--- a/Code/gml_GlobalScript_scr_healallitemspell.gml
+++ b/Code/gml_GlobalScript_scr_healallitemspell.gml
@@ -14,7 +14,16 @@
                 type = 3;
                 damage = arg0;
             }
+            var full = false;
+            if (instance_exists(obj_joker))
+            {
+                full = global.hp[obj_joker.hpchar[myself]] >= global.maxhp[obj_joker.hpchar[myself]];
+            }
+            else
+            {
-            if (global.hp[global.char[myself]] >= global.maxhp[global.char[myself]])
+                full = global.hp[global.char[myself]] >= global.maxhp[global.char[myself]];
+            }
+            if (full)
             {
                 with (dmgwr)
                 {
