--- a/Code/gml_Object_obj_endbattle_Create_0.gml
+++ b/Code/gml_Object_obj_endbattle_Create_0.gml
@@ -4,35 +4,39 @@
 {
     destroy = 1;
 }
-count = 0;
 global.tension = 0;
+c[0] = -4;
+c[1] = -4;
+c[2] = -4;
+havechar[0] = 1;
+havechar[1] = 0;
+havechar[2] = 0;
 if (global.specialbattle == 0 || global.specialbattle == 3)
 {
-    c[0] = scr_dark_marker(global.heromakex[0], global.heromakey[0], spr_krisr_dark);
-    if (instance_exists(global.cinstance[0]))
-    {
-        count += 1;
-        c[1] = scr_dark_marker(global.heromakex[1], global.heromakey[1], global.cinstance[0].rsprite);
-    }
-    if (instance_exists(global.cinstance[1]))
-    {
-        count += 1;
-        c[2] = scr_dark_marker(global.heromakex[2], global.heromakey[2], global.cinstance[1].rsprite);
-    }
-    for (i = 0; i < (count + 1); i += 1)
+    for (i = 0; i < 3; i += 1)
     {
+        var csprite, targetx, targety;
         if (i == 0)
         {
-            c[i].direction = point_direction(c[i].x, c[i].y, obj_mainchara.x, obj_mainchara.y);
-            c[i].speed = point_distance(c[i].x, c[i].y, obj_mainchara.x, obj_mainchara.y) / 10;
-            c[i].depth = 200 - (i * 20);
-        }
-        if (i == 1 || i == 2)
-        {
-            c[i].direction = point_direction(c[i].x, c[i].y, global.cinstance[i - 1].x, global.cinstance[i - 1].y);
-            c[i].speed = point_distance(c[i].x, c[i].y, global.cinstance[i - 1].x, global.cinstance[i - 1].y) / 10;
-            c[i].depth = 200 - (i * 20);
-        }
+            csprite = obj_mainchara.visual_info.rsprite;
+            targetx = obj_mainchara.x;
+            targety = obj_mainchara.y;
+        }
+        else if (instance_exists(global.cinstance[i - 1]))
+        {
+            csprite = global.cinstance[i - 1].rsprite;
+            targetx = global.cinstance[i - 1].x;
+            targety = global.cinstance[i - 1].y;
+        }
+        else
+        {
+            continue;
+        }
+        havechar[i] = 1;
+        c[i] = scr_dark_marker(global.heromakex[i], global.heromakey[i], csprite);
+        c[i].direction = point_direction(c[i].x, c[i].y, targetx, targety);
+        c[i].speed = point_distance(c[i].x, c[i].y, targetx, targety) / 10;
+        c[i].depth = 200 - (i * 20);
     }
     alarm[0] = 10;
 }
