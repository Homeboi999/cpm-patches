--- a/Code/gml_Object_obj_lancerboss2_Draw_0.gml
+++ b/Code/gml_Object_obj_lancerboss2_Draw_0.gml
@@ -62,4 +62,9 @@
     flash = 0;
 }
 becomeflash = 0;
+draw_set_color(c_white);
+draw_text(x, y - 125, sparecon);
+draw_text(x, y - 100, sparecount);
+draw_text(x, y - 50, bubble);
+draw_text(x, y - 25, bubbleactive);
 
