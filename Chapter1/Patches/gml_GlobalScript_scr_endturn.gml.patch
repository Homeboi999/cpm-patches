--- a/Code/gml_GlobalScript_scr_endturn.gml
+++ b/Code/gml_GlobalScript_scr_endturn.gml
@@ -25,8 +25,13 @@
         instance_destroy();
     }
     global.attacking = 0;
+    var noactors = 1;
     for (i = 0; i < 3; i += 1)
     {
+        if (global.acting[i] == 1)
+        {
+            noactors = 0;
+        }
         if (global.charauto[global.char[i]] == 1 && global.hp[global.char[i]] > 0)
         {
             if (global.monster[2] == 1)
@@ -47,7 +52,7 @@
             global.attacking = 1;
         }
     }
-    if (global.acting[0] == 0)
+    if (noactors == 1 && !(instance_exists(obj_lancerboss2) && scr_havechar(2) && scr_susiemode()))
     {
         scr_attackphase();
     }
@@ -55,6 +60,11 @@
     {
         global.charturn = 3;
         global.myfight = 3;
+        global.currentactingchar = 0;
+        if (global.acting[0] != 1)
+        {
+            scr_nextact();
+        }
     }
 }
 
