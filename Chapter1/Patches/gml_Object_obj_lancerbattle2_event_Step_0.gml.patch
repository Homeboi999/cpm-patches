--- a/Code/gml_Object_obj_lancerbattle2_event_Step_0.gml
+++ b/Code/gml_Object_obj_lancerbattle2_event_Step_0.gml
@@ -20,6 +20,10 @@
     __view_set(e__VW.XView, 0, 200);
     global.interact = 1;
     image_speed = 0;
+    with (obj_caterpillarchara)
+    {
+        visible = 0;
+    }
     with (obj_mainchara)
     {
         visible = 0;
@@ -28,6 +32,8 @@
     {
         cutscene = 1;
     }
+    var __cam = view_get_camera(0);
+    heroy = camera_get_view_y(__cam) + 140;
     global.encounterno = 20;
     scr_encountersetup(global.encounterno);
     s = scr_dark_marker(500, __view_get(e__VW.YView, 0) - 20, spr_susied_dark_unhappy);
@@ -42,12 +48,12 @@
 if (con == 2)
 {
     global.interact = 1;
-    if (s.y >= (global.heromakey[0] - 4))
+    if (s.y >= (heroy - 4))
     {
         with (s)
         {
             scr_halt();
-            y = global.heromakey[0];
+            y = other.heroy;
         }
         con = 3;
         alarm[4] = 30;
@@ -646,7 +652,7 @@
 }
 if (con == 43)
 {
-    if (s.x <= (global.heromakex[0] + 2))
+    if (s.x <= (__view_get(e__VW.XView, 0) + 122))
     {
         with (s)
         {
@@ -701,7 +707,7 @@
         instance_destroy();
     }
     global.encounterno = 20;
-    global.specialbattle = 0;
+    global.specialbattle = 1;
     global.flag[9] = 0;
     global.interact = 1;
     con = 51;
@@ -715,35 +721,50 @@
     {
         visible = 0;
     }
+    with (obj_caterpillarchara)
+    {
+        visible = 0;
+    }
     with (obj_mainchara)
     {
         visible = 0;
+    }
+}
+if (con == 51)
+{
+    if (instance_exists(obj_battlecontroller) && obj_battlecontroller.victoried == 1 && !instance_exists(obj_writer))
+    {
+        con = 53;
+        spared = true;
     }
 }
 if (con == 53)
 {
+    if (instance_exists(obj_herosusie))
+    {
-    s = scr_dark_marker(obj_herosusie.x, obj_herosusie.y, spr_susieb_attack_serious);
+        s = scr_dark_marker(obj_herosusie.x, obj_herosusie.y, spr_susieb_attack_serious);
+    }
+    else
+    {
+        s = scr_dark_marker(__view_get(e__VW.XView, 0) + 120, heroy, spr_susieb_attack_serious);
+    }
     with (s)
     {
         scr_depth();
         image_index = 5;
     }
     visible = 1;
+    if (spared)
+    {
+        sprite_index = spr_lancer_battle;
+    }
+    else
+    {
-    sprite_index = spr_lancer_battle_hurt;
+        sprite_index = spr_lancer_battle_hurt;
+    }
     global.fighting = 0;
-    with (obj_battlecontroller)
-    {
-        instance_destroy();
-    }
-    with (obj_herosusie)
-    {
-        instance_destroy();
-    }
-    with (obj_herokris)
-    {
-        instance_destroy();
-    }
-    with (obj_tensionbar)
+    snd_free_all();
+    with (obj_heroparent)
     {
         instance_destroy();
     }
@@ -759,11 +780,26 @@
     {
         instance_destroy();
     }
+    if (!spared)
+    {
+        with (obj_battlecontroller)
+        {
+            instance_destroy();
+        }
+        with (obj_tensionbar)
+        {
+            instance_destroy();
+        }
-    dm = instance_create(x + 30, y + 40, obj_dmgwriter);
+        dm = instance_create(x + 30, y + 40, obj_dmgwriter);
-    dm.type = 1;
+        dm.type = 1;
-    dm.damage = 0;
+        dm.damage = 0;
-    con = 54;
+        con = 54;
-    alarm[4] = 120;
+        alarm[4] = 120;
+    }
+    else
+    {
+        con = 56;
+    }
 }
 if (con == 55)
 {
