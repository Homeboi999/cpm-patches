--- a/Code/gml_Object_obj_werewire_enemy_Step_0.gml
+++ b/Code/gml_Object_obj_werewire_enemy_Step_0.gml
@@ -449,7 +449,7 @@
     if (acting == 2 && actcon == 0)
     {
         var simultext = (simultotal == 1) ? stringsetloc("* You jiggled your body. The wire loosened in turn!/%", "obj_werewire_enemy_slash_Step_0_gml_399_0") : stringsetloc("* You jiggled your body!/%", "obj_werewire_enemy_slash_Step_0_gml_399_1");
-        scr_act_charsprite("kris", spr_krisb_wiggle, 0.25, 1);
+        scr_act_charsprite_simul("kris", 1436, 0.25, 1);
         msgset(0, simultext);
         scr_mercyadd(myself, 50);
         scr_simultext("kris");
@@ -480,15 +480,19 @@
             target = instance_create(x, y, obj_werewire_throwtarget);
             target.maker = id;
         }
+        var krisinstance = global.charinstance[global.currentactingchar];
+        var susieinstance = global.charinstance[scr_findchar_actpartner(2)];
-        with (obj_herokris)
+        with (krisinstance)
         {
             visible = 0;
         }
-        with (obj_herosusie)
+        with (susieinstance)
         {
             visible = 0;
         }
-        throwsus = instance_create(obj_herosusie.x, obj_herosusie.y, obj_werewire_throwkris);
+        throwsus = instance_create(susieinstance.x, susieinstance.y, obj_werewire_throwkris);
+        throwsus.krishero = krisinstance;
+        throwsus.susiehero = susieinstance;
         scr_battletext_default();
         actcon = 14;
     }
@@ -537,13 +541,9 @@
     if (actcon == 10 && !instance_exists(obj_writer))
     {
     }
-    if (actcon == 1 && !instance_exists(obj_writer))
-    {
-        scr_act_charsprite_end();
-        scr_nextact();
-    }
     if (actcon == 20 || actconsus == 20 || actconral == 20 || actconnoe == 20)
     {
+        scr_nextact_simul();
         if (scr_terminate_writer())
         {
             actconsus = -1;
@@ -551,6 +551,11 @@
             actconnoe = -1;
             actcon = 1;
         }
+    }
+    if (actcon == 1 && !instance_exists(obj_writer))
+    {
+        scr_act_charsprite_end();
+        scr_nextact();
     }
 }
 
