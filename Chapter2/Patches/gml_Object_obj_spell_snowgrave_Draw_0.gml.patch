--- a/Code/gml_Object_obj_spell_snowgrave_Draw_0.gml
+++ b/Code/gml_Object_obj_spell_snowgrave_Draw_0.gml
@@ -18,7 +18,11 @@
         fncon = 0;
     }
 }
+if (!sneowait)
+{
-timer++;
+    timer++;
+}
+var sneo_finalphase = instance_exists(obj_spamton_neo_enemy) && obj_spamton_neo_enemy.savemeactcon > 0;
 if (altpath == 1)
 {
     if (fncon == 0)
@@ -29,12 +33,22 @@
             fntimer = 0;
             fncon = 0.5;
             mus_volume(global.batmusic[1], 0, 90);
+            if (sneo_finalphase)
+            {
+                with (obj_spamton_neo_enemy)
+                {
+                    global.myfight = 3;
+                    actcon = 107;
+                    alarm[4] = 90;
+                }
+                sneowait = true;
+            }
         }
     }
     if (fncon == 0.5)
     {
         fntimer++;
-        if (fntimer >= 120)
+        if (fntimer >= 120 && !sneowait)
         {
             with (fn)
             {
@@ -45,18 +59,21 @@
             fncon = 0.8;
             fntimer = 0;
             amplitude = 0;
-            with (obj_berdlyb2_enemy)
+            if (instance_exists(obj_berdlyb2_enemy))
             {
+                with (obj_berdlyb2_enemy)
+                {
-                visible = 0;
+                    visible = 0;
+                }
+                fb = scr_dark_marker(obj_berdlyb2_enemy.x, obj_berdlyb2_enemy.y, spr_berdlyb_idle_shocked);
+                fb.depth = obj_berdlyb2_enemy.depth;
             }
-            fb = scr_dark_marker(obj_berdlyb2_enemy.x, obj_berdlyb2_enemy.y, spr_berdlyb_idle_shocked);
-            fb.depth = obj_berdlyb2_enemy.depth;
         }
     }
     if (fncon == 0.8)
     {
         fntimer++;
-        if (fntimer >= 30)
+        if (fntimer >= 30 || sneo_finalphase)
         {
             fntimer = 0;
             fncon = 1;
@@ -123,7 +140,18 @@
 }
 draw_background_tiled_ext(bg_snowfall, snowspeed / 1.5, timer * 6, 2, 2, c_white, bgalpha);
 draw_background_tiled_ext(bg_snowfall, snowspeed, timer * 8, 2, 2, c_white, bgalpha * 2);
+if (sneo_finalphase && timer >= 0)
+{
+    if (timer <= 10 && bgalpha < 0.5)
+    {
+        bgalpha += 0.05;
+    }
+    else if (timer <= 45 && bgalpha < 0.65)
+    {
+        bgalpha += 0.01;
+    }
+}
-if (timer <= 10 && timer >= 0)
+else if (timer <= 10 && timer >= 0)
 {
     if (bgalpha < 0.5)
     {
@@ -136,6 +164,10 @@
 }
 if (timer == 1)
 {
+    if (sneo_finalphase)
+    {
+        sneodone = true;
+    }
     audio_play_sound(snd_snowgrave, 50, 0);
 }
 if (timer >= 20 && timer <= (75 + (altpath * 30)))
@@ -157,9 +189,42 @@
 }
 if (timer == 70 && altpath == 1)
 {
+    if (instance_exists(fb))
+    {
-    fb.sprite_index = spr_berdly_ice;
+        fb.sprite_index = spr_berdly_ice;
-    fb.x -= 22;
+        fb.x -= 22;
-    fb.y -= 48;
+        fb.y -= 48;
+    }
+}
+if (sneodone)
+{
+    if (timer >= 60)
+    {
+        with (obj_fountainkris_ch2_sideb)
+        {
+            if (instance_exists(blackall) && blackall.image_alpha < 1)
+            {
+                blackall.image_alpha += 0.05;
+            }
+        }
+    }
+    if (timer >= 90 && bgalpha > 0)
+    {
+        bgalpha -= 0.025;
+        with (obj_spamton_neo_enemy)
+        {
+            if (actcon <= 111)
+            {
+                actcon = 113;
+                alarm[4] = 15;
+            }
+        }
+    }
+    if (bgalpha <= 0)
+    {
+        instance_destroy();
+    }
+    exit;
 }
 if (timer == (95 + (altpath * 30)) && damage > 0 && global.fighting == 1)
 {
@@ -190,7 +255,7 @@
                         __of = scr_oflash();
                     }
                 }
-                if (altpath)
+                if (altpath && instance_exists(fb))
                 {
                     with (fb)
                     {
@@ -219,14 +284,16 @@
 {
     if (altpath)
     {
-        with (obj_berdlyb2_enemy)
+        if (instance_exists(obj_berdlyb2_enemy))
         {
+            with (obj_berdlyb2_enemy)
+            {
-            sidebcon = 1;
+                sidebcon = 1;
+            }
+            obj_berdlyb2_enemy.fn = fn;
+            obj_berdlyb2_enemy.fb = fb;
         }
-        obj_berdlyb2_enemy.fn = fn;
-        obj_berdlyb2_enemy.fb = fb;
     }
-    instance_destroy();
 }
 
 enum e__VW
