--- a/Code/gml_Object_obj_heart_Draw_0.gml
+++ b/Code/gml_Object_obj_heart_Draw_0.gml
@@ -23,13 +23,30 @@
             draw_sprite_ext(spr_yheart_charge, 0, (x + 9) - xx, (y + 10) - yy, 4 - ((z_charge * 2) / 35), 4 - ((z_charge * 2) / 35), 0, c_white, z_charge / 5);
         }
     }
-    if (global.chapter == 2 && instance_exists(obj_spamton_neo_enemy) && obj_spamton_neo_enemy.bigshotcount > 0)
+    if (global.chapter == 2 && instance_exists(obj_spamton_neo_enemy) && (obj_spamton_neo_enemy.bigshotcount > 0 || obj_spamton_neo_enemy.multishotcount > 0))
     {
         superchargetimer++;
+        var alpha;
+        if (obj_spamton_neo_enemy.bigshotcount > 0 && obj_spamton_neo_enemy.multishotcount > 0)
+        {
+            alpha = 0.25;
+        }
+        else
+        {
+            alpha = 0.5;
+        }
+        if (obj_spamton_neo_enemy.bigshotcount > 0)
+        {
-        var scale = abs(sin(superchargetimer / 10));
+            var scale = abs(sin(superchargetimer / 10));
-        draw_sprite_ext(sprite_index, 0, x - (scale * 10), y - (scale * 10), 1 + scale, 1 + scale, image_angle, c_orange, 0.5);
+            draw_sprite_ext(sprite_index, 0, x - (scale * 10), y - (scale * 10), 1 + scale, 1 + scale, image_angle, c_orange, 0.5);
-        scale = abs(sin(z_charge_nolimit / 14));
+            scale = abs(sin(z_charge_nolimit / 14));
-        draw_sprite_ext(sprite_index, 0, x - 2 - (scale * 10), y - 2 - (scale * 10), 1.2 + scale, 1.2 + scale, image_angle, c_orange, 0.5);
+            draw_sprite_ext(sprite_index, 0, x - 2 - (scale * 10), y - 2 - (scale * 10), 1.2 + scale, 1.2 + scale, image_angle, c_orange, alpha);
+        }
+        if (obj_spamton_neo_enemy.multishotcount > 0)
+        {
+            var scale = 0.5 * abs(cos(superchargetimer / 10));
+            draw_sprite_ext(spr_yellowheart, 2, x - 2 - (scale * 10), y - 2 - (scale * 10), 1.2 + scale, 1.2 + scale, image_angle, image_blend, 0.5);
+        }
     }
 }
 draw_self();
