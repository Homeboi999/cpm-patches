--- a/Code/gml_Object_obj_berdlyb_enemy_Step_0.gml
+++ b/Code/gml_Object_obj_berdlyb_enemy_Step_0.gml
@@ -263,47 +263,89 @@
     }
     if (acting == 2 && actcon == 0)
     {
-        if (actingsus == 1 && actingral == 1)
+        switch (global.currentactingchar)
+        {
+            case 0:
+                kriscoaster = 1;
+                bump0 = 1;
+                break;
+            case 1:
+                suscoaster = 1;
+                bump1 = 1;
+                break;
+            case 2:
+                ralcoaster = 1;
+                bump2 = 1;
+                break;
+        }
+        i = global.currentactingchar + 1;
+        while (i < 3)
+        {
+            if ((acting == 2 && global.char[i] == 1) || (actingsus == 1 && global.char[i] == 2) || (actingral == 1 && global.char[i] == 3) || (actingnoe == 1 && global.char[i] == 4))
+            {
+                switch (i)
+                {
+                    case 0:
+                        kriscoaster = 1;
+                        bump0 = 1;
+                        break;
+                    case 1:
+                        suscoaster = 1;
+                        bump1 = 1;
+                        break;
+                    case 2:
+                        ralcoaster = 1;
+                        bump2 = 1;
+                        break;
+                }
+            }
+            i++;
+        }
+        if (bump0 && bump1 && bump2)
         {
             dialoguecon = 1;
         }
-        else if (actingsus == 1 && actingral != 1)
-        {
-            msgsetloc(0, "* You and Susie will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_130_0");
-            scr_battletext_default();
-        }
-        else if (actingsus != 1 && actingral == 1)
-        {
-            msgsetloc(0, "* You and Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_131_0");
-            scr_battletext_default();
+        else if ((bump0 + bump1 + bump2) == 2)
+        {
+            if (actingsus == 1 && actingral != 1 && actingnoe != 1 && rideactgo == 0)
+            {
+                msgsetloc(0, "* You and Susie will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_130_0");
+                scr_battletext_default();
+            }
+            else if (actingsus != 1 && actingral == 1 && actingnoe != 1 && rideactgo == 0)
+            {
+                msgsetloc(0, "* You and Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_131_0");
+                scr_battletext_default();
+            }
+            else if (actingsus != 1 && actingral != 1 && actingnoe == 1 && rideactgo == 0)
+            {
+                msgset(0, "* You and Noelle will attempt to bump into Berdly's car!/%");
+                scr_battletext_default();
+            }
+            else if (actingsus != 1 && actingral != 1 && actingnoe != 1 && rideactgo == 0)
+            {
+                msgsetloc(0, "* You will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_132_0");
+                scr_battletext_default();
+            }
         }
         else
         {
             msgsetloc(0, "* You will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_132_0");
             scr_battletext_default();
         }
-        kriscoaster = 1;
         acting = 0;
         actcon = 1;
         rideactgo = 1;
-        if (actingsus == 2)
-        {
-            suscoaster = 1;
-        }
-        if (actingral == 2)
-        {
-            ralcoaster = 1;
-        }
     }
     if (acting == 3 && actcon == 0)
     {
-        dialoguecon = 1;
         acting = 0;
         actcon = 1;
         rideactgo = 1;
         kriscoaster = 1;
         suscoaster = 1;
         ralcoaster = 1;
+        dialoguecon = 1;
     }
     if (acting == 4 && actcon == 0)
     {
@@ -318,6 +360,21 @@
     }
     if (actingsus == 1 && actconsus == 1)
     {
+        switch (global.currentactingchar)
+        {
+            case 0:
+                kriscoaster = 1;
+                bump0 = 1;
+                break;
+            case 1:
+                suscoaster = 1;
+                bump1 = 1;
+                break;
+            case 2:
+                ralcoaster = 1;
+                bump2 = 1;
+                break;
+        }
         if (bumpactstarted == 1)
         {
             acting = 0;
@@ -327,33 +384,84 @@
         }
         else
         {
-            if (actingral != 1 && rideactgo == 0)
+            i = global.currentactingchar + 1;
+            while (i < 3)
+            {
+                if ((acting == 2 && global.char[i] == 1) || (actingsus == 1 && global.char[i] == 2) || (actingral == 1 && global.char[i] == 3) || (actingnoe == 1 && global.char[i] == 4))
+                {
+                    switch (i)
+                    {
+                        case 0:
+                            kriscoaster = 1;
+                            bump0 = 1;
+                            break;
+                        case 1:
+                            suscoaster = 1;
+                            bump1 = 1;
+                            break;
+                        case 2:
+                            ralcoaster = 1;
+                            bump2 = 1;
+                            break;
+                    }
+                }
+                i++;
+            }
+            if (bump0 && bump1 && bump2)
+            {
+                dialoguecon = 1;
+            }
+            else if ((bump0 + bump1 + bump2) == 2)
+            {
+                if (acting == 2 && actingral != 1 && actingnoe != 1 && rideactgo)
+                {
+                    msgsetloc(0, "* You and Susie will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_130_0");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingral == 1 && actingnoe != 1 && rideactgo == 0)
+                {
+                    msgsetloc(0, "* Susie and Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_187_0");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingral != 1 && actingnoe == 1 && rideactgo == 0)
+                {
+                    msgset(0, "* Susie and Noelle will attempt to bump into Berdly's car!/%");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingral != 1 && actingnoe != 1 && rideactgo == 0)
+                {
+                    msgsetloc(0, "* Susie will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_186_0");
+                    scr_battletext_default();
+                }
+            }
+            else
             {
                 msgsetloc(0, "* Susie will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_186_0");
                 scr_battletext_default();
             }
-            if (actingral == 1 && rideactgo == 0)
-            {
-                msgsetloc(0, "* Susie and Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_187_0");
-                scr_battletext_default();
-            }
             acting = 0;
             actcon = 1;
             rideactgo = 1;
             actconsus = 0;
-            suscoaster = 1;
-            if (acting == 2)
-            {
-                kriscoaster = 1;
-            }
-            if (actingral == 2)
-            {
-                ralcoaster = 1;
-            }
         }
     }
     if (actingral == 1 && actconral == 1)
     {
+        switch (global.currentactingchar)
+        {
+            case 0:
+                kriscoaster = 1;
+                bump0 = 1;
+                break;
+            case 1:
+                suscoaster = 1;
+                bump1 = 1;
+                break;
+            case 2:
+                ralcoaster = 1;
+                bump2 = 1;
+                break;
+        }
         if (bumpactstarted == 1)
         {
             acting = 0;
@@ -363,7 +471,57 @@
         }
         else
         {
-            if (actingsus != 1 && rideactgo == 0)
+            i = global.currentactingchar + 1;
+            while (i < 3)
+            {
+                if ((acting == 2 && global.char[i] == 1) || (actingsus == 1 && global.char[i] == 2) || (actingral == 1 && global.char[i] == 3) || (actingnoe == 1 && global.char[i] == 4))
+                {
+                    switch (i)
+                    {
+                        case 0:
+                            kriscoaster = 1;
+                            bump0 = 1;
+                            break;
+                        case 1:
+                            suscoaster = 1;
+                            bump1 = 1;
+                            break;
+                        case 2:
+                            ralcoaster = 1;
+                            bump2 = 1;
+                            break;
+                    }
+                }
+                i++;
+            }
+            if (bump0 && bump1 && bump2)
+            {
+                dialoguecon = 1;
+            }
+            else if ((bump0 + bump1 + bump2) == 2)
+            {
+                if (acting == 2 && actingsus != 1 && actingnoe != 1 && rideactgo == 0)
+                {
+                    msgsetloc(0, "* You and Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_131_0");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingsus == 1 && actingnoe != 1 && rideactgo == 0)
+                {
+                    msgsetloc(0, "* Susie and Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_187_0");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingsus != 1 && actingnoe == 1 && rideactgo == 0)
+                {
+                    msgset(0, "* Ralsei and Noelle will attempt to bump into Berdly's car!/%");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingsus != 1 && actingnoe != 1 && rideactgo == 0)
+                {
+                    msgsetloc(0, "* Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_221_0");
+                    scr_battletext_default();
+                }
+            }
+            else
             {
                 msgsetloc(0, "* Ralsei will attempt to bump into Berdly's car!/%", "obj_berdlyb_enemy_slash_Step_0_gml_221_0");
                 scr_battletext_default();
@@ -372,15 +530,93 @@
             actcon = 1;
             rideactgo = 1;
             actconral = 0;
-            ralcoaster = 1;
-            if (acting == 2)
-            {
+        }
+    }
+    if (actingnoe == 1 && actconnoe == 1)
+    {
+        switch (global.currentactingchar)
+        {
+            case 0:
                 kriscoaster = 1;
-            }
-            if (actingsus == 2)
-            {
+                bump0 = 1;
+                break;
+            case 1:
                 suscoaster = 1;
+                bump1 = 1;
+                break;
+            case 2:
+                ralcoaster = 1;
+                bump2 = 1;
+                break;
+        }
+        if (bumpactstarted == 1)
+        {
+            acting = 0;
+            actcon = 1;
+            actconnoe = 0;
+            actingnoe = 0;
+        }
+        else
+        {
+            i = global.currentactingchar + 1;
+            while (i < 3)
+            {
+                if ((acting == 2 && global.char[i] == 1) || (actingsus == 1 && global.char[i] == 2) || (actingral == 1 && global.char[i] == 3) || (actingnoe == 1 && global.char[i] == 4))
+                {
+                    switch (i)
+                    {
+                        case 0:
+                            kriscoaster = 1;
+                            bump0 = 1;
+                            break;
+                        case 1:
+                            suscoaster = 1;
+                            bump1 = 1;
+                            break;
+                        case 2:
+                            ralcoaster = 1;
+                            bump2 = 1;
+                            break;
+                    }
+                }
+                i++;
             }
+            if (bump0 && bump1 && bump2)
+            {
+                dialoguecon = 1;
+            }
+            else if ((bump0 + bump1 + bump2) == 2)
+            {
+                if (acting == 2 && actingsus != 1 && actingral != 1 && rideactgo == 0)
+                {
+                    msgset(0, "* You and Noelle will attempt to bump into Berdly's car!/%");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingsus == 1 && actingral != 1 && rideactgo == 0)
+                {
+                    msgset(0, "* Susie and Noelle will attempt to bump into Berdly's car!/%");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingsus != 1 && actingral == 1 && rideactgo == 0)
+                {
+                    msgset(0, "* Susie and Ralsei will attempt to bump into Berdly's car!/%");
+                    scr_battletext_default();
+                }
+                else if (acting == 0 && actingsus != 1 && actingral != 1 && rideactgo == 0)
+                {
+                    msgset(0, "* Noelle will attempt to bump into Berdly's car!/%");
+                    scr_battletext_default();
+                }
+            }
+            else
+            {
+                msgset(0, "* Noelle will attempt to bump into Berdly's car!/%");
+                scr_battletext_default();
+            }
+            acting = 0;
+            actcon = 1;
+            rideactgo = 1;
+            actconnoe = 0;
         }
     }
     if (dialoguecon != 0)
@@ -409,7 +645,7 @@
         if (global.mercymod[myself] > 99)
         {
         }
-        else if ((global.currentactingchar == 0 && actingsus == 0 && actingral == 0 && rideactgo == 1) || (global.currentactingchar == 1 && actingral == 0 && rideactgo == 1) || (global.currentactingchar == 2 && rideactgo == 1))
+        else if ((global.currentactingchar == 0 && bump1 == 0 && bump2 == 0 && rideactgo == 1) || (global.currentactingchar == 1 && bump2 == 0 && rideactgo == 1) || (global.currentactingchar == 2 && rideactgo == 1))
         {
             if (mrboneswildride == 1)
             {
@@ -422,7 +658,7 @@
             {
                 if (HeroID == 0 && obj_berdlyb_enemy.kriscoaster == 1)
                 {
-                    sprite_index = spr_krisb_idle;
+                    sprite_index = global.charinstance[0].idlesprite;
                     image_index = 0;
                     image_speed = 0.25;
                 }
@@ -431,7 +667,7 @@
             {
                 if (HeroID == 1 && obj_berdlyb_enemy.suscoaster == 1)
                 {
-                    sprite_index = spr_susieb_idle;
+                    sprite_index = global.charinstance[1].idlesprite;
                     image_index = 0;
                     image_speed = 0.25;
                 }
@@ -440,7 +676,7 @@
             {
                 if (HeroID == 2 && obj_berdlyb_enemy.ralcoaster == 1)
                 {
-                    sprite_index = spr_ralsei_idle;
+                    sprite_index = global.charinstance[2].idlesprite;
                     image_index = 0;
                     image_speed = 0.25;
                 }
@@ -474,6 +710,9 @@
             kriscoaster = 0;
             suscoaster = 0;
             ralcoaster = 0;
+            bump0 = 0;
+            bump1 = 0;
+            bump2 = 0;
         }
         else
         {
